# Base URL of the FastAPI backend the UI calls.
# - Local dev: leave as /api (Vite proxies /api -> http://localhost:8000).
# - Production: set to the deployed API origin, e.g. https://api.example.com
VITE_API_URL=/api
